---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - graphite-api
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: graphite-api
    version: latest

properties:
  graphite_api:
    cache:
      type: filesystem
      options:
        dir: '/var/vcap/sys/tmp/'
